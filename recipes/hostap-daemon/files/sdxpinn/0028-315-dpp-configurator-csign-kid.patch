From 27e69eb828e508d56992434d9c22b739d99798be Mon Sep 17 00:00:00 2001
From: Xinyue Ling <quic_xinyling@quicinc.com>
Date: Tue, 30 Apr 2024 14:57:07 +0800
Subject: [PATCH 28/41] 315-dpp-configurator-csign-kid

Change-Id: I812641137306b997852f6b53a7fba120e3b5dd70
Signed-off-by: Chenming Huang <quic_chenhuan@quicinc.com>
---
 hostapd/ctrl_iface.c  |  5 +++++
 hostapd/hostapd_cli.c | 10 +++++++++
 src/common/dpp.c      | 52 +++++++++++++++++++++++++++++++++++++++++++
 src/common/dpp.h      |  5 +++++
 src/common/wpa_ctrl.h |  1 +
 5 files changed, 73 insertions(+)

diff --git a/hostapd/ctrl_iface.c b/hostapd/ctrl_iface.c
index b6cbb7c0d1..2d0bcd5ab0 100644
--- a/hostapd/ctrl_iface.c
+++ b/hostapd/ctrl_iface.c
@@ -4458,6 +4458,11 @@ static int hostapd_ctrl_iface_receive_process(struct hostapd_data *hapd,
 			reply_len = -1;
 	} else if (os_strncmp(buf, "DPP_RELAY_REMOVE_CONTROLLER ", 28) == 0) {
 		hostapd_dpp_remove_controller(hapd, buf + 28);
+	} else if (os_strncmp(buf, "DPP_CONFIGURATOR_GET_KID ", 25) == 0) {
+		reply_len = dpp_configurator_get_kid_id(
+			hapd->msg_ctx, hapd->iface->interfaces->dpp,
+			atoi(buf + 25),
+			reply, reply_size);
 #endif /* CONFIG_DPP2 */
 #ifdef CONFIG_DPP3
 	} else if (os_strcmp(buf, "DPP_PUSH_BUTTON") == 0) {
diff --git a/hostapd/hostapd_cli.c b/hostapd/hostapd_cli.c
index 82aa2f1f35..26b4672f99 100644
--- a/hostapd/hostapd_cli.c
+++ b/hostapd/hostapd_cli.c
@@ -1589,6 +1589,13 @@ static int hostapd_cli_cmd_dpp_map_peer_disc_start(struct wpa_ctrl *ctrl,
 {
 	return hostapd_cli_cmd(ctrl, "DPP_MAP_PEER_DISC_START", 1, argc, argv);
 }
+
+static int hostapd_cli_cmd_dpp_configurator_get_kid(struct wpa_ctrl *ctrl,
+						    int argc, char *argv[])
+{
+	return hostapd_cli_cmd(ctrl, "DPP_CONFIGURATOR_GET_KID", 1, argc, argv);
+}
+
 #endif /* CONFIG_DPP2 */
 
 
@@ -1876,6 +1883,9 @@ static const struct hostapd_cli_cmd hostapd_cli_commands[] = {
 	{ "dpp_map_peer_disc_start", hostapd_cli_cmd_dpp_map_peer_disc_start,
 	  NULL,
 	  "= start MAP peer disc" },
+	{ "dpp_configurator_get_kid", hostapd_cli_cmd_dpp_configurator_get_kid,
+	  NULL,
+	  "<id> = Get DPP configurator's csign hash" },
 #endif /* CONFIG_DPP2 */
 #ifdef CONFIG_DPP3
 	{ "dpp_push_button", hostapd_cli_cmd_dpp_push_button, NULL,
diff --git a/src/common/dpp.c b/src/common/dpp.c
index a2f3b7532c..57654e52a9 100644
--- a/src/common/dpp.c
+++ b/src/common/dpp.c
@@ -4135,6 +4135,45 @@ int dpp_configurator_get_key(const struct dpp_configurator *conf, char *buf,
 }
 
 
+int dpp_configurator_get_kid(void *msg_ctx, struct dpp_configurator *conf,
+			     char *buf, size_t buflen)
+{
+	struct wpabuf *csign_pub = NULL;
+	const u8 *addr[1];
+	size_t len[1];
+	int ret = -1;
+	int res;
+	char hex[2 * SHA256_MAC_LEN + 1];
+
+	csign_pub = crypto_ec_key_get_pubkey_point(conf->csign, 1);
+	if (!csign_pub) {
+		wpa_printf(MSG_INFO, "DPP: Failed to extract C-sign-key");
+		return -1;
+	}
+
+	/* kid = SHA256(ANSI X9.63 uncompressed C-sign-key) */
+	addr[0] = wpabuf_head(csign_pub);
+	len[0] = wpabuf_len(csign_pub);
+	res = sha256_vector(1, addr, len, conf->kid_hash);
+	wpabuf_free(csign_pub);
+	if (res < 0) {
+		wpa_printf(MSG_DEBUG,
+			   "DPP: Failed to derive kid for C-sign-key");
+		return -1;
+	}
+
+	wpa_hexdump(MSG_DEBUG, "DPP: kid = SHA256(uncompressed C-sign key)",
+	conf->kid_hash, SHA256_MAC_LEN);
+	if (conf->kid)
+		ret = wpa_snprintf_hex(buf, buflen, conf->kid_hash, sizeof(conf->kid_hash));
+
+	wpa_snprintf_hex(hex, sizeof(hex), conf->kid_hash, sizeof(conf->kid_hash));
+	wpa_msg(msg_ctx, MSG_INFO, DPP_EVENT_CONFIGURATOR_CSIGN_KID "%s", hex);
+
+	return ret;
+}
+
+
 static int dpp_configurator_gen_kid(struct dpp_configurator *conf)
 {
 	struct wpabuf *csign_pub = NULL;
@@ -5354,6 +5393,19 @@ int dpp_configurator_get_key_id(struct dpp_global *dpp, unsigned int id,
 	return dpp_configurator_get_key(conf, buf, buflen);
 }
 
+int dpp_configurator_get_kid_id(void *msg_ctx, struct dpp_global *dpp,
+				unsigned int id, char *buf,
+				size_t buflen)
+{
+	struct dpp_configurator *conf;
+
+	conf = dpp_configurator_get_id(dpp, id);
+	if (!conf)
+		return -1;
+
+	return dpp_configurator_get_kid(msg_ctx, conf, buf, buflen);
+}
+
 
 int dpp_map_get_jwk_csign(struct dpp_authentication *auth, const char *cmd, char *jwk)
 {
diff --git a/src/common/dpp.h b/src/common/dpp.h
index d37875c37b..4269766537 100644
--- a/src/common/dpp.h
+++ b/src/common/dpp.h
@@ -672,6 +672,8 @@ const char * dpp_akm_str(enum dpp_akm akm);
 const char * dpp_akm_selector_str(enum dpp_akm akm);
 int dpp_configurator_get_key(const struct dpp_configurator *conf, char *buf,
 			     size_t buflen);
+int dpp_configurator_get_kid(void *msg_ctx, struct dpp_configurator *conf,
+			     char *buf, size_t buflen);
 void dpp_configurator_free(struct dpp_configurator *conf);
 int dpp_configurator_own_config(struct dpp_authentication *auth,
 				const char *curve, int ap);
@@ -757,6 +759,9 @@ int dpp_configurator_set(struct dpp_global *dpp, const char *cmd);
 int dpp_configurator_remove(struct dpp_global *dpp, const char *id);
 int dpp_configurator_get_key_id(struct dpp_global *dpp, unsigned int id,
 				char *buf, size_t buflen);
+int dpp_configurator_get_kid_id(void *msg_ctx, struct dpp_global *dpp,
+				unsigned int id, char *buf,
+				size_t buflen);
 int dpp_map_get_jwk_csign(struct dpp_authentication *auth, const char *cmd,
 			  char *jwk);
 int dpp_map_bss_conf_resp(struct dpp_authentication *auth, const char *cmd);
diff --git a/src/common/wpa_ctrl.h b/src/common/wpa_ctrl.h
index bbdd00743b..a3601b597a 100644
--- a/src/common/wpa_ctrl.h
+++ b/src/common/wpa_ctrl.h
@@ -237,6 +237,7 @@ extern "C" {
 #define DPP_MAP_NETACCESSKEY_JWK "DPP-MAP-NETACCESSKEY-JWK "
 #define DPP_EVENT_JWK_CSIGN "DPP-JWK-CSIGN "
 #define DPP_MAP_EVENT_C_SIGN_KEY "DPP-MAP-C-SIGN-KEY "
+#define DPP_EVENT_CONFIGURATOR_CSIGN_KID "DPP-CONFIGURATOR-CSIGN-KID "
 #define DPP_EVENT_PB_STATUS "DPP-PB-STATUS "
 #define DPP_EVENT_PB_RESULT "DPP-PB-RESULT "
 #define DPP_EVENT_RELAY_NEEDS_CONTROLLER "DPP-RELAY-NEEDS-CONTROLLER "
-- 
2.17.1

