From f016c86138d60a28984c178ca7618a8778611739 Mon Sep 17 00:00:00 2001
From: Hu Wang <quic_huw@quicinc.com>
Date: Tue, 7 May 2024 02:56:03 -0700
Subject: [PATCH 03/10] MAP: Replace athX to wlanX at compile time

Replace athX to wlanX at compile time.

CRs-Fixed: 3893994
Change-Id: If17a88dfd5a3d85b999fa3d6466610782ad26616
Signed-off-by: Chenming Huang <quic_chenhuan@quicinc.com>
---
 map.c       | 96 ++++++++++++++++++++++++++---------------------------
 sigma_dut.h |  1 +
 2 files changed, 49 insertions(+), 48 deletions(-)

diff --git a/map.c b/map.c
index e248b90..af8eb02 100644
--- a/map.c
+++ b/map.c
@@ -608,17 +608,17 @@ static bool map_is_onboard(struct sigma_dut *dut)
 	char *ssid = "wf_OpenWrt";
 
 	if (!dut->map_ath0_onboard) {
-		if (map_is_wlanif_onboard(dut, "ath0", ssid))
+		if (map_is_wlanif_onboard(dut, WLANIF"0", ssid))
 			dut->map_ath0_onboard = true;
 	}
 
 	if (!dut->map_ath1_onboard) {
-		if (map_is_wlanif_onboard(dut, "ath1", ssid))
+		if (map_is_wlanif_onboard(dut, WLANIF"1", ssid))
 			dut->map_ath1_onboard = true;
 	}
 
 	if (!dut->map_ath2_onboard) {
-		if (map_is_wlanif_onboard(dut, "ath2", ssid))
+		if (map_is_wlanif_onboard(dut, WLANIF"2", ssid))
 			dut->map_ath2_onboard = true;
 	}
 
@@ -644,9 +644,9 @@ static bool map_is_ifname(struct sigma_dut *dut, const char *mac,
 	if (s < 0)
 		return rc;
 
-	if (strcmp(ifname, "ath0") == 0)
+	if (strcmp(ifname, WLANIF"0") == 0)
 		ifname_temp = "wifi0";
-	else if (strcmp(ifname, "ath1") == 0)
+	else if (strcmp(ifname, WLANIF"1") == 0)
 		ifname_temp = "wifi1";
 	else
 		ifname_temp = "wifi2";
@@ -715,7 +715,7 @@ static int map_get_bssid_ifname(struct sigma_dut *dut, const char *bssid,
 	while ((fgets(line1, sizeof(line1), file) != NULL) && !found) {
 		token_p = strtok_r(line1, " ", &saveptr);
 		if (token_p) {
-			if (strstr(token_p, "ath")) {
+			if (strstr(token_p, WLANIF)) {
 				strlcpy(ifname, token_p, strlen(token_p) + 1);
 				if (fgets(line1, sizeof(line1), file) == NULL) {
 					pclose(file);
@@ -859,20 +859,20 @@ static int map_get_bsta_bssid(struct sigma_dut *dut, char *bssid)
 	if (dut->map_bh24g || dut->map_bh5g || dut->map_bh5gh) {
 		if (num_radio == 3) {
 			if (dut->map_bh24g)
-				snprintf(ifname, sizeof(ifname), "ath%d1",
+				snprintf(ifname, sizeof(ifname), WLANIF"%d1",
 					 radio_id[2]);
 			else if (dut->map_bh5g)
-				snprintf(ifname, sizeof(ifname), "ath%d1",
+				snprintf(ifname, sizeof(ifname), WLANIF"%d1",
 					 radio_id[1]);
 			else
-				snprintf(ifname, sizeof(ifname), "ath%d1",
+				snprintf(ifname, sizeof(ifname), WLANIF"%d1",
 					 radio_id[0]);
 		} else {
 			if (dut->map_bh24g)
-				snprintf(ifname, sizeof(ifname), "ath%d1",
+				snprintf(ifname, sizeof(ifname), WLANIF"%d1",
 					 radio_id[1]);
 			else if (dut->map_bh5g)
-				snprintf(ifname, sizeof(ifname), "ath%d1",
+				snprintf(ifname, sizeof(ifname), WLANIF"%d1",
 					 radio_id[0]);
 		}
 		rc = map_find_bsta_bssid(dut, ifname, bssid);
@@ -1146,12 +1146,12 @@ int map_dpp_get_local_bootstrap(struct sigma_dut *dut, struct sigma_conn *conn,
 		*success = 0;
 
 	/* remove all old key */
-	snprintf(buf, sizeof(buf), "hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_bootstrap_remove '*'",
+	snprintf(buf, sizeof(buf), "hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_bootstrap_remove '*'",
 		    radio_id[index], radio_id[index]);
 	run_system(dut, buf);
 
 	/*get the bootstrap key*/
-	snprintf(buf, sizeof(buf), "hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_configurator_add curve=P-256",
+	snprintf(buf, sizeof(buf), "hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_configurator_add curve=P-256",
 		      radio_id[index], radio_id[index]);
 	sigma_dut_print(dut, DUT_MSG_DEBUG, "Running '%s'", buf);
 	map_bootstrap_key = popen(buf, "r");
@@ -1168,7 +1168,7 @@ int map_dpp_get_local_bootstrap(struct sigma_dut *dut, struct sigma_conn *conn,
 	}
 
 	key_val = atoi(line);
-	snprintf(buf, sizeof(buf), "hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_configurator_get_key %d",
+	snprintf(buf, sizeof(buf), "hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_configurator_get_key %d",
 		 radio_id[index], radio_id[index], key_val);
 	sigma_dut_print(dut, DUT_MSG_DEBUG, "Running '%s'", buf);
 	map_bootstrap_key = popen(buf, "r");
@@ -1191,7 +1191,7 @@ int map_dpp_get_local_bootstrap(struct sigma_dut *dut, struct sigma_conn *conn,
 	}
 
 	/* get URI value */
-	snprintf(buf, sizeof(buf), "hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_bootstrap_gen type=qrcode key=%s",
+	snprintf(buf, sizeof(buf), "hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_bootstrap_gen type=qrcode key=%s",
 		 radio_id[index], radio_id[index], line);
 	sigma_dut_print(dut, DUT_MSG_DEBUG, "Running '%s'", buf);
 	map_bootstrap_gen = popen(buf, "r");
@@ -1208,7 +1208,7 @@ int map_dpp_get_local_bootstrap(struct sigma_dut *dut, struct sigma_conn *conn,
 	}
 
 	uri_val = atoi(line);
-	snprintf(buf, sizeof(buf), "hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_bootstrap_get_uri %d",
+	snprintf(buf, sizeof(buf), "hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_bootstrap_get_uri %d",
 		 radio_id[index], radio_id[index], uri_val);
 	sigma_dut_print(dut, DUT_MSG_DEBUG, "Running '%s'", buf);
 	map_bootstrap_get_uri = popen(buf, "r");
@@ -1240,7 +1240,7 @@ int map_dpp_get_local_bootstrap(struct sigma_dut *dut, struct sigma_conn *conn,
 	if (map_bootstrap_key) {
 		pclose(map_bootstrap_key);
 		run_system_wrapper(dut,
-			"hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_configurator_remove %d",
+			"hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_configurator_remove %d",
 						      radio_id[index], radio_id[index], key_val);
 	}
 	if (success)
@@ -1255,7 +1255,7 @@ out_err:
 	if (map_bootstrap_key) {
 		pclose(map_bootstrap_key);
 		run_system_wrapper(dut,
-			"hostapd_cli -i ath%d -p /var/run/hostapd-wifi%d dpp_configurator_remove %d",
+			"hostapd_cli -i "WLANIF"%d -p /var/run/hostapd-wifi%d dpp_configurator_remove %d",
 						      radio_id[index], radio_id[index], key_val);
 	}
 
@@ -1333,11 +1333,11 @@ static int map_dpp_deauth(struct sigma_dut *dut, const char *macAddr)
 		if (ifa->ifa_addr == NULL)
 			continue;
 
-		if (strstr(ifa->ifa_name, "ath")) {
+		if (strstr(ifa->ifa_name, WLANIF)) {
 			sigma_dut_print(dut, DUT_MSG_DEBUG,"%-8s\n", ifa->ifa_name);
-			if (strstr(ifa->ifa_name, "ath2"))
+			if (strstr(ifa->ifa_name, WLANIF"2"))
 				hw_name = "wifi2";
-			else if (strstr(ifa->ifa_name, "ath1"))
+			else if (strstr(ifa->ifa_name, WLANIF"1"))
 				hw_name = "wifi1";
 			else
 				hw_name = "wifi0";
@@ -5201,11 +5201,11 @@ static int cmd_dev_reset_default(struct sigma_dut *dut, struct sigma_conn *conn,
 			dut->ap_channel = 149;
 			sigma_dut_print(dut, DUT_MSG_DEBUG, "ctlr: after rpacd,%d", radio_id[0]);
 						if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi2") == 0)
-				dut->hostapd_ifname = "ath2";
+				dut->hostapd_ifname = WLANIF"2";
 			else if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi1") == 0)
-				dut->hostapd_ifname = "ath1";
+				dut->hostapd_ifname = WLANIF"1";
 			else
-				dut->hostapd_ifname = "ath0";
+				dut->hostapd_ifname = WLANIF"0";
 						sigma_dut_print(dut, DUT_MSG_DEBUG,"ctlr: after dut->hostapd_ifname =%s", dut->hostapd_ifname);
 
 			map_enable_lan(dut);
@@ -5457,32 +5457,32 @@ static int cmd_dev_reset_default(struct sigma_dut *dut, struct sigma_conn *conn,
 			dut->ap_channel = 6;
 
 			if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi2") == 0)
-				dut->hostapd_ifname = "ath2";
+				dut->hostapd_ifname = WLANIF"2";
 			else if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi1") == 0)
-				dut->hostapd_ifname = "ath1";
+				dut->hostapd_ifname = WLANIF"1";
 			else
-				dut->hostapd_ifname = "ath0";
+				dut->hostapd_ifname = WLANIF"0";
 
 			if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi0") == 0)
-				ath_set_assoc_disallow(dut, "ath0", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"0", "Disable");
 			if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi1") == 0)
-				ath_set_assoc_disallow(dut, "ath1", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"1", "Disable");
 			if (sigma_radio_ifname[radio_id[0]] && strcmp(sigma_radio_ifname[radio_id[0]], "wifi2") == 0)
-				ath_set_assoc_disallow(dut, "ath2", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"2", "Disable");
 
 			if (sigma_radio_ifname[radio_id[1]] && strcmp(sigma_radio_ifname[radio_id[1]], "wifi0") == 0)
-				ath_set_assoc_disallow(dut, "ath0", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"0", "Disable");
 			if (sigma_radio_ifname[radio_id[1]] && strcmp(sigma_radio_ifname[radio_id[1]], "wifi1") == 0)
-				ath_set_assoc_disallow(dut, "ath1", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"1", "Disable");
 			if (sigma_radio_ifname[radio_id[1]] && strcmp(sigma_radio_ifname[radio_id[1]], "wifi2") == 0)
-				ath_set_assoc_disallow(dut, "ath2", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"2", "Disable");
 
 			if (sigma_radio_ifname[radio_id[2]] && strcmp(sigma_radio_ifname[radio_id[2]], "wifi0") == 0)
-				ath_set_assoc_disallow(dut, "ath0", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"0", "Disable");
 			if (sigma_radio_ifname[radio_id[2]] && strcmp(sigma_radio_ifname[radio_id[2]], "wifi1") == 0)
-				ath_set_assoc_disallow(dut, "ath1", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"1", "Disable");
 			if (sigma_radio_ifname[radio_id[2]] && strcmp(sigma_radio_ifname[radio_id[2]], "wifi2") == 0)
-				ath_set_assoc_disallow(dut, "ath2", "Disable");
+				ath_set_assoc_disallow(dut, WLANIF"2", "Disable");
 
 			map_disable_lan(dut);
 			// run_system_wrapper(dut, "ifconfig eth1 down");
@@ -5582,20 +5582,20 @@ int cmd_dev_map_get_parameter(struct sigma_dut *dut, struct sigma_conn *conn,
 				if (num_radio == 3) {
 					if (dut->map_bh24g)
 						snprintf(ifname, sizeof(ifname),
-							 "ath%d1", radio_id[2]);
+							 WLANIF"%d1", radio_id[2]);
 					else if (dut->map_bh5g)
 						snprintf(ifname, sizeof(ifname),
-							 "ath%d1", radio_id[1]);
+							 WLANIF"%d1", radio_id[1]);
 					else
 						snprintf(ifname, sizeof(ifname),
-							 "ath%d1", radio_id[0]);
+							 WLANIF"%d1", radio_id[0]);
 				} else {
 					if (dut->map_bh24g)
 						snprintf(ifname, sizeof(ifname),
-							 "ath%d1", radio_id[1]);
+							 WLANIF"%d1", radio_id[1]);
 					else if (dut->map_bh5g)
 						snprintf(ifname, sizeof(ifname),
-							 "ath%d1", radio_id[0]);
+							 WLANIF"%d1", radio_id[0]);
 				}
 				s = socket(AF_INET, SOCK_DGRAM, 0);
 				if (s < 0) {
@@ -5695,7 +5695,7 @@ int cmd_dev_map_get_parameter(struct sigma_dut *dut, struct sigma_conn *conn,
 			val = get_param(cmd, "STA_MACAddress");
 			if (!val)
 				goto map_get_param_error;
-	   //str*cat(cmd, val); //snprintf(ifname, sizeof(ifname), "ath12");  /*to fix*/
+	   //str*cat(cmd, val); //snprintf(ifname, sizeof(ifname), WLANIF"12");  /*to fix*/
 			memset(resp, 0, sizeof(resp));
 			memcpy(resp, "PTK,", 4);
 			resp[4] = '\0';
@@ -6459,7 +6459,7 @@ static int cmd_dev_set_config(struct sigma_dut *dut, struct sigma_conn *conn,
 
 			dut->map_bh5g = dut->map_bh24g = dut->map_bh5gh = false;
 
-			if (map_is_ifname(dut, val, "ath0")) {
+			if (map_is_ifname(dut, val, WLANIF"0")) {
 				map_disable_lan(dut);
 				// run_system_wrapper(dut, "ifconfig eth1
 				// down");
@@ -6519,7 +6519,7 @@ static int cmd_dev_set_config(struct sigma_dut *dut, struct sigma_conn *conn,
 				    dut, "uci set "
 					 "ezmesh.config."
 					 "NonRelayingEthernetInterface="
-					 "ath01");
+					 WLANIF"01");
 				run_system_wrapper(dut,
 						   INIT_SCRIPT_PATH"mcsd stop");
 				run_system_wrapper(dut,
@@ -6563,7 +6563,7 @@ static int cmd_dev_set_config(struct sigma_dut *dut, struct sigma_conn *conn,
 					}
 				}
 
-			} else if (map_is_ifname(dut, val, "ath1")) {
+			} else if (map_is_ifname(dut, val, WLANIF"1")) {
 				map_disable_lan(dut);
 				// run_system_wrapper(dut, "ifconfig eth1
 				// down");
@@ -6625,7 +6625,7 @@ static int cmd_dev_set_config(struct sigma_dut *dut, struct sigma_conn *conn,
 				    dut, "uci set "
 					 "ezmesh.config."
 					 "NonRelayingEthernetInterface="
-					 "ath11");
+					 WLANIF"11");
 				run_system_wrapper(dut,
 						   INIT_SCRIPT_PATH"mcsd stop");
 				run_system_wrapper(dut,
@@ -6670,7 +6670,7 @@ static int cmd_dev_set_config(struct sigma_dut *dut, struct sigma_conn *conn,
 					}
 				}
 
-			} else if (map_is_ifname(dut, val, "ath2")) {
+			} else if (map_is_ifname(dut, val, WLANIF"2")) {
 					map_disable_lan(dut);
 					// run_system_wrapper(dut, "ifconfig
 					// eth1 down");
@@ -6746,7 +6746,7 @@ static int cmd_dev_set_config(struct sigma_dut *dut, struct sigma_conn *conn,
 					    dut, "uci set "
 						 "ezmesh.config."
 						 "NonRelayingEthernetInterface="
-						 "ath21");
+						 WLANIF"21");
 					run_system_wrapper(
 					    dut, INIT_SCRIPT_PATH"mcsd stop");
 					run_system_wrapper(
diff --git a/sigma_dut.h b/sigma_dut.h
index 03ca4de..758e125 100644
--- a/sigma_dut.h
+++ b/sigma_dut.h
@@ -110,6 +110,7 @@ struct sigma_dut;
 
 #define NAN_AWARE_IFACE "wifi-aware0"
 #define BROADCAST_ADDR "255.255.255.255"
+#define WLANIF "wlan"
 
 /* Set default operating channel width 80 MHz */
 #define VHT_DEFAULT_OPER_CHWIDTH AP_80_VHT_OPER_CHWIDTH
-- 
2.17.1

